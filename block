import random

class block:

    def __init(self, stim_list):
        self.stim_list = stim_list
        self.success_count = 0
        self.failure_count = 0

    stim_list = random.shuffle(self.stim_list)

    for stim in stim_list:


        if trial.get_trial_data[1] == 1
            self.succes_count+1
        else:
            self.failure_count+1
